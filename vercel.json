{
    "version": 2,
    "buildCommand": "npx prisma generate",
    "routes": [
        {
            "src": "/api/users",
            "dest": "/api/users.ts"
        },
        {
            "src": "/(.*)",
            "dest": "/api/index.ts"
        }
    ]
}